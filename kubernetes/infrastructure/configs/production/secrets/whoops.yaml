apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: whoops-creds
  namespace: whoops
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-kv-store
    kind: ClusterSecretStore
  target:
    name: whoops-creds
  data:
    - secretKey: username
      remoteRef:
        key: apps
        property: WHOOPS_DB_USERNAME
    - secretKey: password
      remoteRef:
        key: apps
        property: WHOOPS_DB_PASSWORD
    - secretKey: SQLALCHEMY_DATABASE_URI
      remoteRef:
        key: apps
        property: WHOOPS_SQLALCHEMY_DATABASE_URI
    - secretKey: CLIENT_ID
      remoteRef:
        key: apps
        property: WHOOP_CLIENT_ID
    - secretKey: CLIENT_SECRET
      remoteRef:
        key: apps
        property: WHOOP_CLIENT_SECRET
