apiVersion: apps/v1
kind: Deployment
metadata:
  name: nfs-provisioner-home-apps
  namespace: nfs-provisioner
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nfs-provisioner-home-apps
  template:
    metadata:
      labels:
        app: nfs-provisioner-home-apps
    spec:
      serviceAccountName: nfs-client-provisioner
      containers:
        - name: nfs-provisioner
          image: registry.k8s.io/sig-storage/nfs-subdir-external-provisioner:v4.0.2
          volumeMounts:
            - name: nfs-root
              mountPath: /persistentvolumes
          env:
            - name: PROVISIONER_NAME
              value: nfs-home-apps.k8s.io
            - name: NFS_SERVER
              value: server01.kryoseu.com
            - name: NFS_PATH
              value: /tank/home-apps
      volumes:
        - name: nfs-root
          nfs:
            server: server01.kryoseu.com
            path: /tank/home-apps
